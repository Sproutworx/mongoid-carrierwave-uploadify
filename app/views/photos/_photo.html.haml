.photo[photo]
  = link_to(image_tag(photo.image.thumb.url), [@story, photo]) if photo.image.thumb.url
  %br
  = link_to 'edit', edit_story_photo_path(@story, photo)
  |
  = link_to 'delete', [@story, photo], :confirm => 'Are you sure?', :method => :delete
  %br
/  = photo.title